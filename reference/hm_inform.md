# Emit package messages honoring options(healthmarkers.verbose)

Emit package messages honoring options(healthmarkers.verbose)

## Usage

``` r
hm_inform(level = c("inform", "info", "debug"), msg = NULL)
```

## Arguments

- level:

  "inform" or "debug"

- msg:

  character(1)
