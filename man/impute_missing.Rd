% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute_missing.R
\name{impute_missing}
\alias{impute_missing}
\title{Impute missing values in a data.frame or tibble}
\usage{
impute_missing(
  data,
  method = c("mean", "median", "zero", "constant"),
  cols = NULL,
  constant = NULL
)
}
\arguments{
\item{data}{A \code{data.frame} or \code{tibble} containing missing values.}

\item{method}{Character; one of:
\itemize{
\item \code{"mean"}     : replace NAs with the column mean
\item \code{"median"}   : replace NAs with the column median
\item \code{"zero"}     : replace NAs with \code{0}
\item \code{"constant"} : replace NAs with the single value given in \code{constant}
}}

\item{cols}{Optional character vector of column names to impute.
Defaults to \emph{all} numeric columns in \code{data} that contain at least one \code{NA}.}

\item{constant}{Numeric; single value to use when \code{method = "constant"}.}
}
\value{
A \code{data.frame} (or \code{tibble}) of the same dimensions as \code{data},
with the specified columns’ missing values imputed.
}
\description{
A simple, column-wise imputer for numeric data.  Non-numeric
columns are left untouched.
}
\examples{
df <- data.frame(
  x = c(1, NA, 3),
  y = c(NA, 2, NA),
  z = c("a", "b", NA)
)
# default: mean-impute all numeric columns
impute_missing(df)

# zero-impute only column “x”
impute_missing(df, method = "zero", cols = "x")

# constant imputation
impute_missing(df, method = "constant", constant = 99)

}
