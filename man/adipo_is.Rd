% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adipo_is.R
\name{adipo_is}
\alias{adipo_is}
\title{Calculate adipose based insulin sensitivity indices}
\usage{
adipo_is(data, col_map, normalize = "none", verbose = FALSE)
}
\arguments{
\item{data}{A data.frame or tibble containing at least the columns mapped by \code{col_map}.}

\item{col_map}{Named list mapping:
\itemize{
\item \code{G0} → fasting glucose (mmol/L)
\item \code{I0} → fasting insulin (pmol/L)
\item \code{TG} → triglycerides (mmol/L)
\item \code{HDL_c} → HDL cholesterol (mmol/L)
\item \code{FFA} → free fatty acids (mmol/L)
\item \code{waist} → waist circumference (cm)
\item \code{bmi} → body‐mass index (kg/m²)
}}

\item{normalize}{One of \code{c("none","z","inverse","range","robust")}—method to scale each index.}

\item{verbose}{Logical; if \code{TRUE}, prints a progress message.}
}
\value{
A tibble with the 10 adipose‐based index columns.
}
\description{
Given fasting glucose & insulin plus lipid and adiposity measures,
computes:
\itemize{
\item Revised_QUICKI
\item VAI_Men_inv
\item VAI_Women_inv
\item TG_HDL_C_inv
\item TyG_inv
\item LAP_Men_inv
\item LAP_Women_inv
\item McAuley_index
\item Adipo_inv
\item Belfiore_inv_FFA
}
}
\examples{
df <- tibble::tibble(
  G0    = 5.5, I0    = 60,
  TG    = 1.2, HDL_c = 1.0,
  FFA   = 0.45, waist = 80, bmi = 24
)
adipo_is(
  df,
  col_map = list(
    G0    = "G0",
    I0    = "I0",
    TG    = "TG",
    HDL_c = "HDL_c",
    FFA   = "FFA",
    waist = "waist",
    bmi   = "bmi"
  ),
  normalize = "none"
)
}
